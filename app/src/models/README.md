# Models

This folder contains the models for the application. 
The models are used to represent the data in the application, and also contain the associated business logic.

The models are organized into two main categories: DTOs (Data Transfer Objects) and domain models. DTOs are used to transfer data between different layers of the application, while domain models represent the core business logic and data of the application.
